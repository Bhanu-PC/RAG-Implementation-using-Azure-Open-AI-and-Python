- Problem with LLM was its trained on specific dataset and cannot fetch realtime data and cannot be retrained every year as its expnesive.
- R: Fetching relevant info from external source like db, database or web
  A: Adding that info to models context
  G: Produce answwer using model knowledge and context
- Retreive the context(doc), augment the query(llm vectors), generate a response
- RAG is a method that combines a retrieval mechanism with a generative language model to enhance the accuracy and relevance of AI-generated responses.
- In this project, you’ll learn how to integrate Azure OpenAI into a Python-based application. Retrieve relevant documents or data using a vector database. Enhance generative AI outputs by combining retrieved data with model-generated responses.

